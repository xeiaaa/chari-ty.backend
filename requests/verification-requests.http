### Import common variables
@import "_common.http"

### Submit a verification request for a group
POST {{baseUrl}}/{{apiPrefix}}/groups/{{groupId}}/verification-request
Authorization: Bearer {{devToken}}
Content-Type: {{contentType}}

{
  "reason": "We are a legitimate nonprofit organization seeking verification to build trust with our donors."
}

### Submit a verification request without reason
POST {{baseUrl}}/{{apiPrefix}}/groups/{{groupId}}/verification-request
Authorization: Bearer {{devToken}}
Content-Type: {{contentType}}

{}

### Admin: Update verification request (admin only)
PATCH {{baseUrl}}/{{apiPrefix}}/admin/group/{{groupId}}/verification-request
Authorization: Bearer {{devToken}}
Content-Type: {{contentType}}

{
  "status": "approved",
  "reason": "Approved after review of documentation. Group meets all verification requirements."
}

### Admin: Reject verification request (admin only)
PATCH {{baseUrl}}/{{apiPrefix}}/admin/group/{{groupId}}/verification-request
Authorization: Bearer {{devToken}}
Content-Type: {{contentType}}

{
  "status": "rejected",
  "reason": "Insufficient documentation provided. Please provide additional verification documents."
}

### Admin: List all verification requests (admin only)
GET {{baseUrl}}/{{apiPrefix}}/admin/verification-requests
Authorization: Bearer {{devToken}}

### Admin: List verification requests with pagination
GET {{baseUrl}}/{{apiPrefix}}/admin/verification-requests?page=1&limit=20
Authorization: Bearer {{devToken}}

### Admin: List verification requests filtered by status
GET {{baseUrl}}/{{apiPrefix}}/admin/verification-requests?status=pending
Authorization: Bearer {{devToken}}

### Admin: List verification requests filtered by group name
GET {{baseUrl}}/{{apiPrefix}}/admin/verification-requests?groupName=charity
Authorization: Bearer {{devToken}}

### Admin: List verification requests with multiple filters
GET {{baseUrl}}/{{apiPrefix}}/admin/verification-requests?status=pending&groupName=charity&page=1&limit=10
Authorization: Bearer {{devToken}}

### Variables (set these in your environment or replace directly)
# @groupId = your_group_id_here
